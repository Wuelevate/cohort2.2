import React from "react";
import ReactDOM from "react-dom/client"; 
import MultiStepForm from "./components/MultiStepForm"; // Adjust the path if needed
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MultiStepForm />
  </React.StrictMode>
);



//entry point